import { createTheme } from '@mui/material'



const theme = createTheme({
    palette: {
        primary: {
            main: '#5f2c3e'
        },
        secondary: {
            main: '#d1adcc'
        }
      },

});
export default theme;